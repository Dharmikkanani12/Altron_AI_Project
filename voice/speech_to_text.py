"""
Speech-to-text — turn the user's voice into text.

Stub. Suggested implementation: `SpeechRecognition` package
(pip install SpeechRecognition), backed by Google's free API for
testing or a local Whisper model for production/offline use.
"""


def listen() -> str:
    # TODO: implement with SpeechRecognition, e.g.:
    #   import speech_recognition as sr
    #   r = sr.Recognizer()
    #   with sr.Microphone() as source:
    #       audio = r.listen(source)
    #   return r.recognize_google(audio)
    raise NotImplementedError("Wire up voice/speech_to_text.py:listen with SpeechRecognition or Whisper")
