"""
Screen — take screenshots for ALTRON to analyze (e.g. spotting an error).

Stub. Suggested implementation: `Pillow` (ImageGrab) or `mss` for speed.
"""


def screenshot(save_path: str = "screenshot.png") -> str:
    # TODO: implement with Pillow, e.g.:
    #   from PIL import ImageGrab
    #   img = ImageGrab.grab()
    #   img.save(save_path)
    raise NotImplementedError("Wire up vision/screen.py:screenshot with Pillow's ImageGrab")
