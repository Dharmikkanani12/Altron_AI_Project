"""
OCR — extract text from images (e.g. reading an error message from a screenshot).

Stub. Suggested implementation: `pytesseract` (requires the Tesseract binary
installed separately) or a cloud OCR API.
"""


def extract_text(image_path: str) -> str:
    # TODO: implement with pytesseract, e.g.:
    #   import pytesseract
    #   from PIL import Image
    #   return pytesseract.image_to_string(Image.open(image_path))
    raise NotImplementedError("Wire up vision/ocr.py:extract_text with pytesseract")
