"""
Camera — capture frames from a webcam for vision tasks.

Stub. Suggested implementation: `opencv-python`.
"""


def capture_frame():
    # TODO: implement with opencv-python, e.g.:
    #   import cv2
    #   cam = cv2.VideoCapture(0)
    #   ok, frame = cam.read()
    #   cam.release()
    #   return frame
    raise NotImplementedError("Wire up vision/camera.py:capture_frame with opencv-python")
