"""
Browser tool — fetch web pages / drive a browser on behalf of agents.

Stub. Suggested implementations:
    - Simple fetch: `requests` + `beautifulsoup4`
    - Full browser control: `playwright` or `selenium`
"""


def fetch_page(url: str) -> str:
    # TODO: implement with requests + BeautifulSoup, e.g.:
    #   import requests
    #   return requests.get(url, timeout=10).text
    raise NotImplementedError("Wire up tools/browser.py:fetch_page with requests or playwright")
